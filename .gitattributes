# 仓库根这一层的行尾锁定。
# 工程/ 那一层另有一份 .gitattributes（子目录的会叠加生效），改行尾规矩去改那一份。
#
# 为什么要有这一份：.gitattributes 的作用域是它所在目录及以下。
# 2026-09-01 把工程件收进 工程/ 之后，那份 .gitattributes 跟着搬走了，
# 根目录剩下的 README.md / 00-说明.md / .gitignore 就没有属性保护了——
# 别人用 Git for Windows 默认的 core.autocrlf=true 克隆时会被自动改写行尾。
* -text
