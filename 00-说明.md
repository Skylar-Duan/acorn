# 橡果 Acorn

> **外层这几个文件夹是给你的，工程件全收进 `工程/` 了，你不用点进去。**
> 下面这张表告诉你该去哪儿。

## 东西都在哪

| | |
|---|---|
| `安装包/` | 双击就装的 exe / 安卓 APK。正式版最新：电脑 v1.16.0（2026-09-23）、安卓 v1.15.0（三端各排各的号）；**「橡果 beta 安装包.exe」是还没发布的测试版**（装上后版本号那里显示 beta），每次做完我直接装到你这台上，这个文件永远是最新那一个 |
| `交付验收/` | **一个大版本一份验收单**：v1 全在 `橡果 v1 怎么验.html` 里，按网站 / 安卓 / 桌面分开看，最新改动在最上面，每条标「已核 / 待核」——你核完跟我说一声，我来改 |
| `方案/` | 给你拍板的东西，一件一个文件：字体方案（已定）、手机端体验方案 + 设计稿 |
| `界面截图/` | 它长什么样，按版本分文件夹 |
| `网站/` | acorn.cdpandas.com/intro/ 介绍页的**真源**（9/3 起从 10 接管）：改这里，跑一条命令就上线；10 那边只是镜像 |
| `userdata/` | **你的账本**（正在用的那份数据，别挪，橡果记的是绝对路径） |
| `TODO.md` | 橡果这条线的待办与状态（原来在 `02-Gadgets/TODO.md` 里，2026-09-04 搬家时一起搬过来） |
| `工程/` | 源码、依赖、构建产物、服务端、文档。给开发看的，你不用看 |

## 你可能在找

| 你想干嘛 | 去哪儿 |
|---|---|
| **装它（电脑）** | [`安装包/`](安装包/) —— 双击就装，装完开始菜单里叫「橡果」 |
| **装它（手机）** | 手机上已有橡果的：设置 → 版本更新，自己下自己装。第一次装：[`安装包/`](安装包/) 里的 `.apk` 传到手机点开（GitHub Releases 那份要等发版才更新） |
| **不装也想用（新）** | 浏览器打开 <https://acorn.cdpandas.com/> 就是橡果本体（旧址 /app/ 会自动跳过来），登录同一个邮箱看到同一份事。iPhone：分享 → 添加到主屏幕，就跟个 App 一样 |
| **验收**（全部 v1：最新改动 + 已发布的 21 个版本，三端分开） | [`交付验收/橡果 v1 怎么验.html`](交付验收/橡果%20v1%20怎么验.html) —— 双击打开；以前一版一份的旧单子收在 `_work/归档/旧验收单/` |
| **看它长什么样** | [`界面截图/`](界面截图/) —— 按日期 / 版本分文件夹，最新的是 `2026-09-23/`（计划页新分组）和 `2026-09-21/`（这一批其余改动）；`v1.14.2/` 是最后一套完整的对外截图（演示数据） |
| **看手机版的设计稿** | [`方案/手机端设计稿/预览/`](方案/手机端设计稿/预览/) —— 9 张画板 + 3 张「质感方向」（方向A/B/C，等你选）；方案说明在 [`方案/手机端方案.md`](方案/手机端方案.md) |
| 看它能干什么 | [`工程/README.md`](工程/README.md)（前半段是人话）|
| 看每个版本改了什么 | [`工程/CHANGELOG.md`](工程/CHANGELOG.md) |
| **看网站上要挂的文案** | [`工程/网站文案.md`](工程/网站文案.md) —— 页面上每句中文以它为准，要改文案改这里；改完同步进 [`网站/index.html`](网站/index.html) 再发 |
| 字体方案（9/2 已定：标题文楷、正文思源黑体） | [`方案/字体方案（已定）.html`](方案/字体方案（已定）.html) —— 当时的四个候选留档 |

## 一句话

待办工具。随手打一句「周五下午3点 提交周报 #工作 @李哥 !高」，日期、清单、需求方、
优先级它全自动认出来。带日历、四象限、习惯打卡、云同步、安卓版。
（番茄钟专注计时这一版整体收起了，代码和历史记录都留着，方案定了再开放。）

不会写那种句子也没关系——「随手记」右边那个 `?` 打开一页举例卡片，照着抄就会
（设置里也有同一个入口）。

## 云同步那部分

服务端已经上线在 `https://acorn.cdpandas.com`，代码在 `工程/server/`。
「查有没有新版」的接口 2026-09-02 起也在线了（电脑版、手机版各一个），安装包放在同一台服务器上，
橡果打开会自己查、自己下、自己装；更新日志弹窗顶上也能手动查。
**这是一个真在跑的线上服务**，动它之前先看 [`TODO.md`](TODO.md)。

橡果的账号是**独立**的，跟 cdpandas 其他板块（门户、SBG 那些）不共用——
只有发验证码的那条邮件通道是同一条。同一个邮箱在两边都要各注册一次。

## 给开发的（你不用看）

`工程/` 那一层才是代码。npm 命令都在 `工程/` 目录下跑，不是在这一层。

| | |
|---|---|
| `工程/src/` | 界面代码 |
| `工程/src-tauri/` | 桌面应用外壳 + 安卓打包配置 |
| `工程/server/` | 云同步服务端 |
| `工程/docs/` | 数据模型变更台账 |
| `工程/tests/`、`工程/scripts/` | 测试和打包脚本 |
| `工程/node_modules/`、`工程/dist/` | 依赖和构建产物，不进代码仓库 |

`交付验收/`、`安装包/`、`界面截图/`、`方案/`、`userdata/` 这五个也不进代码仓库；`网站/` 进（页面跟着版本走）。
发网站（在 `工程/` 下跑，看到 `[5/5] done` 才算上线）：
```
python "../../10-Platform/finance/scripts/publish_utility.py" --tool acorn --src ../网站 --label vX.Y.Z
```
⚠️ `--src ../网站` 这一段**现在不能省**：10 那边脚本里登记的真源路径还是搬家前的 `02-Gadgets/acorn/网站`，
省掉它脚本会退回去发 10 自己那份镜像（等于把旧页面发上线）。已给 10 留言改登记，改完这段就能删。
