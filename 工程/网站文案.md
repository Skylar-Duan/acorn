# 橡果 · 网站文案（cdpandas.com/utilities/acorn）

> 给 10 的页面口径真源。**页面上出现的每一句中文都以本文件为准**，10 只做排版实现，不改字。
> 改文案回来改这里，别在 HTML 里就地改，否则下次刷新又打回去。
>
> 写作纪律（用户 2026-08-31 定）：**去 AI 味，做成吸引人的产品介绍。**
> 不要四联排比、不要给软件安拟人动词（"它自己认""收下""陪伴"）、不要"零门槛/轻松/贴心/一键/智能"、
> 不要破折号接抒情尾巴、不要装饰性 emoji。**讲清楚它替你干什么，比夸它好听有用。**
>
> 最后更新：2026-09-23（桌面 v1.16.0 发版同步：「计划」那张卡换成新分组；「更新日志」那一段改成跟应用里电脑版的日志同一份、由脚本生成，见第三节；
> 下载弹窗 Windows 那行兜底号 1.16.0，安卓那行仍是 1.15.0——三端各排各的号，安卓这次没发）
>
> 2026-09-14 深夜（v1.15.0 加网页版：页首多一颗「打开网页版」按钮，「电脑和手机同一份事」那张卡
> 补了 iPhone 加到主屏幕的两步。改动只有这两处，别的文案一个字没动）
>
> 2026-09-09 深夜（v1.14.3 发版同步，桌面 + 安卓两个包一起发；语法表补了新认得的词；页面文案本身没变，变的只有「版本日志」和读不到接口时的兜底版本号。
> 1.11.1 出过两次包、第二次同号重发不会被客户端提示更新，所以直接换号到 1.12.0，页面上只留 1.12.0 这一段）
> 2026-09-02：v1.10.1 发版同步；用户 9/2 又定了两条：**每次发版网站必须同步**、复核用语要自然 + 商业、去过度宣传、修不易读的句子

---

## 〇、给 10 的执行说明（不是页面文案）

1. **现在就上，桌面版（当前 v1.14.3）。** 下载不再等 GitHub Release：主按钮直接指向更新服务器上的**固定文件名**
   `https://acorn.cdpandas.com/download/windows/Acorn-latest-x64-setup.exe`（永远是最新版，以后发版这个链接不用改）。
   版本号 / 大小 / 发布日期 / 更新说明建议页面加载时读 `https://acorn.cdpandas.com/api/desktop/latest`
   （JSON 字段 `version` `size` `publishedAt` `notes`，已开跨域），读不到就显示写死的 v1.14.3。这样以后每版只有「介绍文案有变」才要改页面。
2. **截图用 `acorn/界面截图/v1.10.1/电脑/`**（路径从 acorn 根算起，也就是本文件的上两层）：演示数据，可对外，9 张。
   建议上 4 张：01 今天、02 更新日志弹窗、05 日历月视图、07 设置折叠。v1.11.x 桌面端样子没变，这批仍然是最新的；
   `v1.14.2/电脑/` 里是最新的四张（今天 / 记一条 / 回收站 / 设置），想换就换（界面这两版没变）。`v1.8/` 那批是用户真实待办，**仍然不能对外**。
3. **安卓包先不上页面**：v1.14.3 有同版安卓包，手机端刚按体验重做完、正在真机验收，Skylar 点头后我再来留言给你安卓段的文案和固定链接
   （届时是 `.../download/android/Acorn-latest-arm64.apk`）。
4. **以后每次发版我都会来 INBOX 留言**，格式固定：版本号 / 更新日志文案 / 截图路径 / 安卓上不上。链接不变，你只改文案。
5. **网页版（App 本体）从 v1.15.0 起由橡果自己发，不走 `publish_utility.py`。**
   2026-09-21 起地址调整：网页版挂根路径 `https://acorn.cdpandas.com/`（原 `/app/`，旧址 301 到根），
   介绍页挪到 `https://acorn.cdpandas.com/intro/`（10 管，acorn-site.inc 只管 `/intro`、`/intro/`、`/utilities/`）。
   网页版的 nginx 段（`/`、`/assets/`、`/icons/`、`/manifest.webmanifest`、`/app` 跳转）写在橡果仓库的
   `server/deploy/nginx-acorn.conf` 里，产物由橡果的 `publish-web.sh` 传到 `/var/www/acorn-web/`。
   **两份配置不要互相加对方的路径**，同名 location 出现两次 nginx 会直接起不来。

---

## 一、页首（hero）

**kicker（小字标签）**

```
工作效率 · 记事清单 · Windows / 安卓 / 网页 三端同步
```

**h1**

```
橡果 Acorn
```

**导语（h1 下面那段，两句，别加第三句）**

```
多数待办工具都假设事情是你自己给自己安排的。橡果不是——它记得每件事是谁交办的，
侧栏按人分栏，一眼看清欠了哪位同事几件事。
```

**示例块（保留现在这个展示方式，它是全页最有说服力的东西）**

```
打一句话进去：

    周五下午3点 提交周报 /工作 @李哥 !高

日期、时间、清单、需求方、重要性，五样一次到位，不用先选再选。
不想记这套写法，输入框下面就是一排按钮，点着选一样。
```

**meta 行（版本与环境）**

```
当前版本 v1.16.0 · Windows / 安卓 / 网页 · 数据存在你自己的机器上
```

> 10 注意：版本号建议从 `api/desktop/latest` 读，别写死也别自己猜。

**CTA**

```
主按钮：下载 Windows 版
  → https://acorn.cdpandas.com/download/windows/Acorn-latest-x64-setup.exe
并排按钮：打开网页版
  → https://acorn.cdpandas.com/
按钮下小字：安装包 26MB，装的时候不需要管理员权限，装完会自己检查更新。
            网页版不用装，浏览器打开就是同一个橡果，登录同一个邮箱看到的是同一份事。
```

> 10 注意：「打开网页版」是并排的第二颗按钮（次按钮样式，不要做成主按钮）。
> 它指向根路径 `/`，是橡果 App 本体，不是另一张介绍页（介绍页自己在 `/intro/`）。

**「反馈」按钮（只给管理员，2026-09-21 加，未发布）**

普通访客看不到这颗按钮，也看不到下面任何一句。认得是管理员（登录着 cdpandas 的管理员账号，或这个浏览器登录着橡果网页版的管理员账号）
才出现在「打开网页版」右边，次按钮样式。实现在 `网站/assets/feedback-admin.js`，改字回来改这里再同步到脚本。

```
按钮：反馈（有没处理的，右上角带数字角标，超过 99 写 99+）
面板标题：反馈 · N 条没处理（全处理完时只写「反馈」）
关闭按钮：×（读屏念「关闭」）
两个页签：未处理 / 全部
每条下面一行小字：邮箱 · 电脑版|安卓|网页版 版本号 · 设备名 · 时间
  时间：今天 14:05 / 昨天 14:05 / 9月20日 14:05 / 往年加年份
每条的状态和按钮：未处理 [标记已处理]   /   已处理 · 时间 · 处理人 [改回未处理]
标记没成功：没改成，稍后再试
读取中：正在读取…
空的：没有要处理的反馈（未处理页） / 还没有人发过反馈（全部页）
翻页按钮：再看更早的（每次 50 条）
读取失败：没读到，关掉再开一次试试（翻页时：没读到，再点一次）
```

---

## 二、「它能做什么」

**section 标题**

```
它能做什么
```

**section 导语（一句，不要排比）**

```
下面这些是橡果每天真正在替你干的事。
```

### 卡片（按这个顺序，h3 是卡片标题）

**1 ｜ 一句话记一件事**

```
不用先选日期、再选清单、再点重要性。把话打完就行：

· 日期：明天 / 下周三 / 8月20日 / 十月底之前
· 时间与循环：下午3点、每周一三五
· /清单 · #标签 · @需求方 · !高

边打边显示解析出来的结果，不对当场改。
```

**2 ｜ 记得每件事是谁交办的**

```
橡果把「为谁做」当成和日期、清单同一级的东西。侧栏按人分栏，
点开某个人就是欠他的全部事情；统计里也各算各的。

如果你的事大多是别人交办的，按人看比按重要性排更管用。
```

**3 ｜ 习惯和待办分开算**

```
喝水、背单词、健身这类重复着做的事，有自己的一屏：
一个大圈点一下就是打卡，旁边是连续多少次和本周七格，点开能看整月日历。

习惯不会逾期。昨天没做就是没做，不会滚成一笔债堵在今天。
忘了打卡可以补，「每周一三五」这种也数得对——连续三次数的是该做的日子，不是自然日。
```

**4 ｜ 计划按「什么时候」分段**

```
今天 / 本周 / 本月 / 半年内 / 更远 / 未安排。不用在一条长列表里翻哪件是这周要交的；写「~半年内 考驾照」，日期就落在半年内那一段的最后一天。
换一个看法，就变成按重要性分成高 / 中 / 低 / 普通四段，每段里再按时间排。
```

**5 ｜ 有子任务的事，按「下一步干什么」排**

```
一件事还剩几个子任务没做，列表里就占几行（「写周报 › 画趋势图」），
母任务先收起来，全做完了它才回来收尾。

子任务没单独填日期和重要性时就等于母任务的：母任务定在周五，
它的子任务周五当天自己出现在「今天」。
```

**6 ｜ 改一整句话，就是改这件事**

```
点开任何一件事，底下那栏已经写好了它的完整一句话。
删掉「!高」就降级，删掉「@李哥」就把人摘了，旁边一个按钮随时还原。
```

**7 ｜ 电脑和手机同一份事**

```
手机上不用装：用 Safari 打开 acorn.cdpandas.com，点底下的「分享」，
再选「添加到主屏幕」，它就跟一个 App 一样待在手机桌面上，打开就是橡果。
安卓的 Chrome 在右上角菜单里，也是「添加到主屏幕」。

一个邮箱就能跨设备。新装的设备登录后直接拿到云端那份；这台设备上已经记了事的话，
登录时会问你是把两边合起来还是用云端那份。同一件事两边都改过，以改得晚的那次为准。

断网、服务器出问题都不影响你继续用，本机那份照常读写。
不注册的话跟以前完全一样，一个字都不会往外传。

登录之后，退出登录会把这台设备上的数据一起清掉，只留云端那一份——
清之前橡果会先跟服务器对一次账，没对上就一条都不清。
```

**8 ｜ 数据在你自己的机器上**

```
默认存在本机，装完就能用。想让数据跟着移动硬盘走，去设置里换个文件夹就行。

保存时先写好副本再替换，写到一半断电也不会碰坏原来那份；每天留一份备份共 30 份，
删掉的东西在回收站里放 30 天，日常的每一步操作都能撤销。

唯一不能撤销的是设置里那两个明确写着「会覆盖 / 会清空」的动作，点之前会让你确认。
```

**9 ｜ 六款主题，各有一幅画**

```
森林的针叶林、海洋的浪、星空的银河、沙漠的落日沙丘、雪山的冰川、南极的极光，
淡淡地垫在任务下面。每款都有深浅两色。
```

**10 ｜ 键盘、右键、拖拽都通**

```
Ctrl+K 命令面板，Alt+Space 在任何地方叫出「记一条」小窗。
任务右键：完成、推明天、改日期、改重要性、移清单、换人、复制、删除。
拖到侧栏「今天」就是改今天，拖到某个人名下就是改需求方。
```

> 10 注意：**不要**在页面上出现「专注 / 番茄钟」——v1.9 起这个功能暂时收起了。
> 现在页面上「九个视图」那张卡里写了「专注」，要改掉。

---

## 三、更新日志区（releases）

**2026-09-23 起不在这儿写了**：介绍页的更新日志跟应用里电脑版点版本号看到的是同一份，
由 `scripts/site-changelog.py` 从 `src/core/changelog-data.json`（desktop，1.9.0 起）和
`../_work/验收单工具/早期版本.json`（desktop，1.0.0 ~ 1.8.0）生成进 `../网站/index.html` 两个标记之间。
写法照 `../_work/约定.md` 第五节（新功能直接点名 + 日常例子，细节合成一张「体验优化」）。

- 发版时：先改 `changelog-data.json`，再在 `工程/` 下跑 `python scripts/site-changelog.py`，然后照常发网站。
- 只列电脑版：介绍页读的是桌面版的更新接口；手机版、网页版的日志在各自 App 设置里「查看更新日志」看。
- 原来这里手写的那份（「某一步也能自己重复了」「手风琴」这些 09-18 被用户批过的说法）整份作废，别再往页面上贴。

---

## 四、要从页面上删掉的旧文案（点名）

| 现在页面上的 | 问题 | 换成 |
|---|---|---|
| 温暖纸感的待办工具。把事情一颗颗收进来，一颗颗处理掉。 | 拟人 + 抒情，没说清它干什么 | 见「一、导语」 |
| 记录零门槛，安排有章法，执行有陪伴，回头有交代。 | 四联排比，典型 AI 腔 | 「下面这些是橡果每天真正在替你干的事。」 |
| 日期、时间、清单、需求方、优先级**全部自己认出来** | 「自己认」是给软件安拟人动词 | 「五样一次到位」 |
| 九个视图，各看各的（含「专注」） | 视图已经重排，专注也收起来了 | 按新卡片 4 / 10 重写 |
| 当前版本 v1.2.1 | 落后七个版本 | 发版时我给参数 |

另外：「**需求方**」这个词页面上照用，全文已统一。软件里那一栏就叫这个，页面改了叫法反而对不上。
但**第一次出现时要带一句解释**（卡片 2 已经写好了：「橡果把『为谁做』当成和日期、清单同一级的东西」），
不能让第一次看到的人自己猜。
